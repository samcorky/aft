[pytest]
# Exclude directories that are not test directories
testpaths = server/tests
norecursedirs = data backups .git .venv node_modules dist build
# Prevent pytest from trying to collect from socket files or other non-test paths
python_files = test_*.py *_test.py
python_classes = Test*
python_functions = test_*
